import React from 'react';
import ReactDOM from 'react-dom/client';
import HomePage from './HomePage';
import './index.css';

ReactDOM.createRoot(document.getElementById('front-home-root')).render(
  <React.StrictMode>
    <HomePage />
  </React.StrictMode>
);